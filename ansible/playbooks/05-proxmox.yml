---
# ansible/playbooks/05-proxmox.yml
# Configures Proxmox nodes post-install

- name: Configure Proxmox nodes
  hosts: proxmox_nodes
  become: true
  gather_facts: true
  roles:
    - proxmox
  tags:
    - proxmox
